export async function handleModal(interaction) {
  // Placeholders for future Modal interactions (e.g. nickname changes)
  return interaction.reply({
    content: 'モダールの送信を受け取りましたが、現在のバージョン(Ver 0.3)では未実装です。',
    ephemeral: true
  });
}
